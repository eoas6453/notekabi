# 工作笔记 · 产品需求文档

> 版本 v1.0 · 定位：轻量、快速、本地优先的个人工作笔记工具

---

## 一、产品定位

**一句话**：给需要沉淀工作经验的人，一个打开就能写、写完就自动存、随时能翻回来的本地笔记本。

**目标用户**：工程师、产品、运营等知识工作者；每天有零散工作记录需求，希望把经验沉淀成可复用资产。

**设计信条**：

| 原则 | 含义 | 具体体现 |
| --- | --- | --- |
| 记录优先 | 从想到到写下，路径必须最短 | 全局快捷键新建、自动保存、不用先选文件夹/命名 |
| 本地优先 | 数据主权归用户 | 一篇一 .md 文件，纯文本，可用任何工具打开 |
| 克制 | 不堆功能 | 首屏可见按钮不超过 5 个，高级功能收进弹窗 |
| 键盘优先 | 手不离键盘 | 15 个常用操作均有快捷键，1 次按键直达 |
| 可迁移 | 不被锁定 | 随时导出 Markdown / Word / PDF，存储目录可换 |

---

## 二、用户场景与痛点

| 场景 | 现状痛点 | 本产品做法 |
| --- | --- | --- |
| 会议/排障时随手记 | 打开软件要等、要选位置、要点保存 | 启动 < 1s，`Ctrl+N` 直接写，停止输入自动保存 |
| 下班前写工作日志 | 每天重复搭框架 | `Ctrl+D` 生成带框架的今日笔记 |
| 月底做复盘 | 翻不到当时记了什么 | 全文搜索 + 标签 + 时间线分组 + 随机回顾 |
| 遇到同类问题 | 明明踩过坑却想不起来 | 双向链接把相关笔记串起来 + 关系图谱 |
| 换电脑 / 怕数据丢 | 云端服务关停、导出难 | 本地 md 文件 + 历史版本 + 一键导出 |
| 给领导汇报 | 笔记要转成正式文档 | 一键导出 Word / PDF |

---

## 三、竞品研究与取舍

研究了 Obsidian / Logseq / Joplin / AppFlowy / SiYuan / Memos-Flomo，逐项评估「是否适合个人工作笔记」：

| 产品 | 值得借鉴 | 为什么不全盘照搬 |
| --- | --- | --- |
| **Obsidian** | 本地 md 文件、双向链接、反向链接面板、命令面板、标签体系 | 插件生态带来复杂度与性能负担；个人工作笔记用不上几百个插件 |
| **Logseq** | 每日笔记（Daily Note）体验、块引用、大纲式书写 | 大纲/块级结构对纯文本 md 兼容性差，迁移成本高；学习曲线陡 |
| **Joplin** | 多端同步、导入导出、笔记本层级 | 编辑与预览分离体验偏传统，界面略重 |
| **AppFlowy** | 块编辑器、Notion 式体验、数据库视图 | 自有数据格式，不是纯 md，导出链路长 |
| **SiYuan** | 块级引用、本地优先、中文社区 | 概念多（块、属性、SQL 查询），上手成本高 |
| **Memos / Flomo** | 快速短记录、时间流、无压力书写 | 只有时间流，缺少长文与整理能力 |

**结论：取各家最"轻"的那一刀**

- 从 Obsidian 拿：本地 md + 双向链接 + 反向链接 + 标签
- 从 Logseq 拿：每日笔记（但用普通 md 而非大纲）
- 从 Flomo 拿：零压力快速记录、随机回顾
- 从 Joplin 拿：导出能力
- **不做的**：插件系统、块级引用、数据库视图、多端同步服务、协同编辑

---

## 四、功能规划

### P0 · MVP（已全部实现）

| 功能 | 说明 |
| --- | --- |
| 笔记 CRUD | 创建、编辑、删除（软删除进回收站） |
| Markdown 编辑与实时预览 | 编辑 / 分屏 / 预览 三档切换 |
| 自动保存 | 停止输入 0.8s 自动落盘，原子写入 |
| 笔记列表 | 按更新时间 / 创建时间 / 标题排序，按天分组 |
| 全文搜索 | 多关键词 AND、`tag:xxx`、短语匹配，关键词高亮 |
| 标签系统 | 行内 `#标签` + 顶部标签管理，点击过滤 |
| 每日笔记 | `Ctrl+D` 自动创建/打开当天笔记 |
| 本地存储 | 一篇一 `.md` 文件 + front-matter 元数据 |
| 导出 | 单篇/全部 → Markdown / Word / PDF |
| 基础设置 | 主题、字号、存储路径、排序 |
| Windows 可执行 | 便携版 zip，解压即用 |

### P1 · 重要增强（已全部实现）

| 功能 | 说明 |
| --- | --- |
| 双向链接 | `[[标题]]`，右侧反向链接面板；不存在的标题可一键创建 |
| 笔记模板 | 每日笔记 / 工作日志 / 复盘 / 项目总结 / 经验卡片 |
| 图片与附件 | 粘贴截图、拖拽文件、按钮选择，存入 attachments/ |
| Markdown 扩展 | 代码块高亮、GFM 表格、任务列表、删除线、高亮 |
| 收藏 / 置顶 | 列表置顶显示 |
| 数据统计 | 笔记数、字数、标签频率、近 14 天活跃度、待办完成率 |
| 随机回顾 | 随机打开旧笔记，偏重 7 天前的笔记 |

### P2 · 进阶（已实现其中 4 项）

| 功能 | 状态 |
| --- | --- |
| 待办看板（汇总所有 checkbox） | ✅ 已实现，可直接勾选回写 |
| 关系图谱 | ✅ 已实现，力导向布局 |
| 多标签组合筛选 | ✅ 已实现 |
| 导入 Markdown 文件夹 | ✅ 已实现 |
| 文件夹/笔记本层级 | ⛔ 刻意不做 —— 用标签替代，避免"该放哪个文件夹"的决策负担 |
| WebDAV 同步 | ⏸ 延后 —— 把存储目录指向网盘目录即可满足 90% 需求 |
| 移动端适配 | ⏸ 延后 —— CSS 变量已就位，架构解耦，后续可快速接入 |

---

## 五、界面结构

```
┌──────────────┬─────────────────────┬──────────────────────────────┬──────────────┐
│  侧边栏 224  │   笔记列表 312      │   编辑区（自适应）            │ 反向链接 260 │
│              │                     │                              │  （可折叠）   │
│  ■ 工作笔记  │  🔍 搜索框          │  📝 标题输入        📌⭐⤓🗑  │              │
│              │  ─────────────────  │  #标签 +标签  创建·更新·字数  │  反向链接(2) │
│ [＋ 新建笔记]│  排序 ▾      12 篇  │  ──────────────────────────  │  ┌────────┐ │
│              │                     │  B I S H • ☑ ❝ </> 🔗 ⊞ 🖼  │  │笔记A   │ │
│ 📝 全部笔记 8│  ── 今天 ──         │                  编辑|分屏|预览│  └────────┘ │
│ ⭐ 收藏    2 │  ▸ 每日笔记         │  ──────────────────────────  │  ┌────────┐ │
│ 📅 每日笔记 3│    09-08 星期一     │                              │  │笔记B   │ │
│ ✅ 待办看板  │    今天做了什么…    │   Markdown 源码   │  实时预览  │  └────────┘ │
│ 🕸️ 关系图谱  │                     │                   │            │              │
│ 📊 数据统计  │  ── 昨天 ──         │                   │            │  本篇引用(1) │
│ 🗑️ 回收站   │  ▸ 项目复盘         │                   │            │  ┌────────┐ │
│              │    教训是…          │                   │            │  │➕ 新笔记│ │
│ 快捷操作     │                     │                   │            │  └────────┘ │
│ ☀️ 今日笔记  │  ▸ 经验卡片         │                   │            │              │
│ 🎲 随机回顾  │    Redis 缓存…      │                   │            │              │
│ 📋 模板新建  │                     │                   │            │              │
│              │                     │                   │            │              │
│ 标签         │                     │                   │            │              │
│ (工作 5)(复盘 3)                   │                   │            │              │
│ (项目 2)(日志 3)                   │                   │            │              │
│              │                     │                   │            │              │
│ ⚙️ 设置      │                     │                   │            │              │
│ 📁 存储路径  │                     │                   │            │              │
├──────────────┴─────────────────────┴──────────────────────────────┴──────────────┤
│ 状态栏：● 已保存 14:32 · 共 12 篇 · 本地运行无需联网                        ⌨      │
└────────────────────────────────────────────────────────────────────────────────┘
```

**设计要点**

- **三栏固定**：侧栏（导航）+ 列表（检索）+ 内容（写作），符合 Notion/Obsidian 用户肌肉记忆
- **反向链接可折叠**：默认展开，窄屏或专注时一键收起，不挤占写作空间
- **专注模式**：`Ctrl+Shift+F` 隐藏侧栏与列表，只留文字
- **状态栏常驻**：随时可见保存状态，消除"到底存没存"的焦虑
- **弹窗收纳高级功能**：设置、统计、模板、快捷键、回收站、导出，主界面保持干净

---

## 六、关键交互细节

### 6.1 记录路径最短化

- 新建笔记后光标自动进入编辑区，可直接打字
- 标题留空时自动取正文首行作为标题（无需先想标题）
- 停止输入 0.8 秒自动保存，状态栏从"正在保存…"变回"已保存 时间"

### 6.2 搜索

- `Ctrl+K` 从任何位置聚焦搜索框
- 支持多关键词（空格分隔，AND 关系）、`tag:工作` 标签过滤、`"精确短语"`
- 标题命中权重 > 标签 > 正文，结果按相关度排序
- 命中词在标题、摘要、正文中统一高亮为黄色底

### 6.3 双向链接

- 输入 `[[` 后可用工具按钮快速插入
- 标题存在 → 蓝色实线下划线；不存在 → 灰色虚线（可点击一键创建）
- 反向链接面板显示引用本篇的笔记 + 引用所在的上下文行

### 6.4 标签

- 两种入口：正文里写 `#标签`（自动识别），或顶部标签框手动添加
- 左侧标签云点击筛选，**支持多选组合**（同时满足）
- 侧栏按使用频率排序，附数量

### 6.5 数据安全

- 自动保存 + 原子写入（tmp → rename）
- 每次保存前归档旧版本到 `backups/<id>/`，保留最近 5 份
- 删除进回收站，可恢复、可彻底清除

---

## 七、视觉规范

| 项 | 规范 |
| --- | --- |
| 配色 | 中性灰底 + 单一强调色（靛蓝 `#4f46e5`），无渐变、无阴影堆砌 |
| 浅色 | 背景 `#fff`，侧栏 `#f7f7f6`，列表 `#fbfbfa`，文字 `#1f2328` |
| 深色 | 背景 `#1b1c1f`，侧栏 `#161719`，文字 `#e5e7eb`，强调色转亮 `#8b95f8` |
| 字体 | 系统无衬线（Segoe UI / 微软雅黑 / PingFang），代码用等宽（Consolas / JetBrains Mono） |
| 圆角 | 卡片 8px，弹窗 12px，标签 pill 全圆角 |
| 间距 | 侧栏 10px、内容区 24px，列表项纵向 9px |
| 动效 | 仅 120~180ms 的 hover/切换过渡，不做花哨动画 |
| 字号 | 正文默认 15px，设置中 13~20px 可调 |

---

## 八、验收标准对照

| 验收项 | 实现方式 | 状态 |
| --- | --- | --- |
| 增删改 + 自动保存 | 0.8s debounce + 原子写 | ✅ |
| Markdown 渲染（代码块/表格/图片粘贴） | 自研渲染器 + 剪贴板图片落盘 | ✅ |
| 全文搜索准确 + 高亮 | 加权评分 + 关键词高亮 | ✅ |
| 标签点击过滤、每日笔记可用 | 多标签组合 + 日期模板 | ✅ |
| 导出全部为 MD/Word/PDF 且格式保留 | zip / 手写 OOXML / Chromium 打印 | ✅ |
| Windows .exe 双击启动 | 便携版 zip（Electron 免安装运行时） | ✅ |
| 浅色/深色主题切换 | CSS 变量 + data-theme | ✅ |
| 无网络依赖 | 零网络请求代码 | ✅ |
| 启动 < 2s | index.json 缓存 + 分批并发读取 | ✅ |
| 核心操作有快捷键 | 15 组快捷键 | ✅ |

---

## 九、关键假设（未与需求方确认，按以下假设推进）

1. **单用户、单机**：不做账号体系与多端同步，存储目录指向网盘目录即可满足同步需求
2. **笔记量级 < 5000 篇**：全文索引放在内存线性扫描即可，不引入索引库
3. **中文为主**：界面全中文，字数统计按 CJK 字符 + 英文单词混合计算
4. **Windows 优先**：只产出 Windows x64 包；macOS/Linux 复用同一套代码重新打包即可
5. **不做文件夹层级**：用标签替代，降低"归类决策成本"
6. **图片以本地文件引用**：不内嵌 base64，避免 .md 文件膨胀
